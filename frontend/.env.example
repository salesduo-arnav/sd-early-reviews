# Local dev overrides (Vite dev server → backend at :3030)
# Not used in Docker — Docker build args come from root .env via docker-compose.yml
VITE_API_BASE_URL=http://localhost:3030/api
VITE_GOOGLE_CLIENT_ID=<SECRET_CLIENT_ID>
STRIPE_PUBLISHABLE_KEY=<SECRET_STRIPE_PUBLISHABLE_KEY>
